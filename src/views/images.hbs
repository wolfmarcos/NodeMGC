
{{!-- 
{{#each b}} <img src="{{b.imageURL}}" alt="">{{/each}}
{{#each b}} <h1>{{b}}</h1>  <img src={{b}} alt=""> {{/each}}
{{images.[0].nomb}}
<br><br>

<br><br>
{{#each images}} {{this}} <br><br>{{/each}}

{{#each images}} <img src={{imageURL}} alt="">{{/each}}
{{#each b}} <h1>{{b}}</h1>  <img src={{b}} alt=""> {{/each}}
ariel

<br><br>

<br><br>
<div>

{{#each images}} <h2>{{title}}</h2> <br><br>{{/each}}
</div> --}}

<a class="btn btn-danger" href="{{cate}}" role="button" target="_blank">{{cate}}</a>
<div class="swiper-container">
    <!-- Additional required wrapper -->
    <div class="swiper-wrapper">
        <!-- Slides -->
        {{#each images}}
         
        {{!-- <img src="{{imageURL}}" alt="" class="swiper-slide"> --}}
        {{!-- <a href="{{imageURL}}" target="_blank" class="btn btn-primary">Ver</a> --}}
        <a href="/image/show/{{_id}}" src="{{imageURL}} title="{{title}}" class="swiper-slide">

            <img src="{{imageURL}}" alt="" class="swiper-slide">
            <li value="{{title}} " class="swiper-wrapper">{{title}}</li>

        </a>
        {{/each}}
    </div>
    <br>
    <br>
    <!-- If we need pagination -->
    <div class="swiper-pagination"></div>
    <!-- If we need navigation buttons -->
    <!-- <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div> -->
    <!-- If we need scrollbar --> 
    <!-- <div class="swiper-scrollbar"></div> -->
</div>

